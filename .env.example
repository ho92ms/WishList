# -----------------------------
# App / Environment
# -----------------------------
# local | prod
ENV=local
LOG_LEVEL=INFO

# -----------------------------
# Database
# -----------------------------
# Local dev: SQLite
# Prod (Render): PostgreSQL (Render provides DATABASE_URL)
DATABASE_URL=sqlite:///./data/app.db

# -----------------------------
# Places Provider (OSM: Nominatim + Overpass)
# -----------------------------
PLACES_PROVIDER=osm

# City -> coordinates
NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org

# POI search
OVERPASS_BASE_URL=https://overpass-api.de/api/interpreter
# Fallback mirror (used if primary times out)
OVERPASS_FALLBACK_URL=https://overpass.kumi.systems/api/interpreter

# Search tuning (smaller = faster, safer)
PLACES_RADIUS_M=1500
PLACES_LIMIT=5
PLACES_HTTP_TIMEOUT_S=45

# Required by OSM/Nominatim usage policy
OSM_USER_AGENT="TravelWishlistStudentApp/1.0 (contact: nemethdavidnak@gmail.com)"

# -----------------------------
# Backend server
# -----------------------------
# Local only – Render overrides host/port automatically
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_BASE_URL=http://127.0.0.1:8000

# -----------------------------
# Automations
# -----------------------------
# Weekly top wishlist calculation
WEEKLY_REPORT_DAYS=7
WEEKLY_REPORT_TOP_N=10
