# -----------------------------
# App / Environment
# -----------------------------
ENV=local
LOG_LEVEL=INFO

# -----------------------------
# Database
# -----------------------------
# Local fejlesztéshez SQLite
DATABASE_URL=sqlite:///./data/app.db

# -----------------------------
# Places Provider
# -----------------------------
PLACES_PROVIDER=osm

# Geocoding provider:
# - local: nominatim (működik lokálban)
# - cloud (Render): open_meteo (stabilabb, nincs API kulcs)
GEOCODER_PROVIDER=open_meteo

# Nominatim (lokál)
NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org

# Open-Meteo geocoding (cloud)
OPENMETEO_GEOCODING_BASE_URL=https://geocoding-api.open-meteo.com/v1

# Overpass (POI keresés)
# Tipp: a kumi gyakran gyorsabb/stabilabb, ezért primary-nek ezt használjuk
OVERPASS_BASE_URL=https://overpass.kumi.systems/api/interpreter
OVERPASS_FALLBACK_URL=https://overpass-api.de/api/interpreter

# Keresési beállítások
PLACES_RADIUS_M=1500
PLACES_LIMIT=6
PLACES_HTTP_TIMEOUT_S=48

# Udvariasság / azonosítás OSM felé (használj valós kontaktot)
OSM_USER_AGENT=TravelWishlistStudentApp/1.0 (contact: nemethdavidnak@gmail.com)

# -----------------------------
# Backend server (local)
# -----------------------------
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_BASE_URL=http://127.0.0.1:8000

# -----------------------------
# Automations
# -----------------------------
WEEKLY_REPORT_DAYS=7
WEEKLY_REPORT_TOP_N=10
